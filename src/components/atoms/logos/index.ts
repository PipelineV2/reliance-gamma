import Vite from "./vite";
import RelianceLogo from "./reliance";

export interface LogosProps {
  width?: number | string;
  height?: number | string;
  className?: string;
}

const Logos = {
  Vite,
  RelianceLogo,
};

export default Logos;
